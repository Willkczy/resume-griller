# Legacy ML pointer

This directory is duplicated historical scaffolding and is not an active ML package.
The authoritative status and future fine-tuning gate are documented in
`ml/README.md` at the repository root.

Do not add training code, data, or model artifacts here.
