# Resume Griller Environment Configuration

# ============== App Settings ==============
DEBUG=true

# ============== LLM Configuration ==============
# Mode: "api" (Claude/OpenAI) or "local" (LoRA model)
LLM_MODE=api

# API Mode Settings
LLM_PROVIDER=anthropic  # "anthropic" or "openai"
ANTHROPIC_API_KEY=your-anthropic-api-key-here
OPENAI_API_KEY=your-openai-api-key-here

# Model names (optional, defaults are set)
# ANTHROPIC_MODEL=claude-sonnet-4-20250514
# OPENAI_MODEL=gpt-4o

# Local Mode Settings (for LoRA model)
# LOCAL_MODEL_DEVICE=cuda  # "cuda", "mps", or "cpu"

# ============== RAG Settings ==============
CHROMA_PERSIST_DIR=./data/chromadb
EMBEDDING_MODEL=all-MiniLM-L6-v2

# ============== File Upload ==============
UPLOAD_DIR=./data/uploads
MAX_UPLOAD_SIZE=10485760  # 10MB in bytes

# ============== Database ==============
DATABASE_URL=sqlite+aiosqlite:///./data/resume_griller.db

# ============== CORS ==============
# Comma-separated list of allowed origins
CORS_ORIGINS=["http://localhost:3000", "http://127.0.0.1:3000"]
```

---

### 📄 檔案 8: 更新 `requirements.txt`

在專案根目錄更新（或建立新的 `backend/requirements.txt`）：
```
# ============== Existing RAG Dependencies ==============
torch>=2.0.0
pdfplumber>=0.10.0
PyMuPDF>=1.23.0
langchain>=0.1.0
chromadb>=0.4.0
sentence-transformers>=2.2.0

# ============== FastAPI Backend ==============
fastapi>=0.109.0
uvicorn[standard]>=0.27.0
python-multipart>=0.0.6
python-dotenv>=1.0.0
pydantic>=2.5.0
pydantic-settings>=2.1.0

# ============== Database ==============
sqlalchemy>=2.0.0
aiosqlite>=0.19.0

# ============== Async Support ==============
httpx>=0.26.0
aiofiles>=23.2.0

# ============== LLM API Clients ==============
anthropic>=0.18.0
openai>=1.12.0

# ============== LoRA Model (for local inference) ==============
transformers>=4.36.0
peft>=0.7.0
accelerate>=0.25.0